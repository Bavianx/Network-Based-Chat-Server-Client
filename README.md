# Network-Based Chat Server & Client (Python)

A multi-client network-based chat application built in Python using TCP sockets and threading. The server allows multiple clients to connect, choose a nickname, and exchange real-time messages with timestamps.

---

##  Features
- Client–Server architecture using TCP
- Multiple clients supported simultaneously
- Nickname-based messaging
- Real-time chat with timestamps
- `/quit` command to safely disconnect
- Server-side broadcasting
- Threaded communication for concurrency

---

##  Technologies Used
- **Language:** Python 3
- **Networking:** Socket Programming (TCP/IP)
- **Concurrency:** Threading
- **Encoding:** ASCII / UTF-8
